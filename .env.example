# Keep all credentials server-side. Never commit a real value.
NOKIA_NAC_API_KEY=
DECISION_RATE_LIMIT=12
DECISION_BUDGET_MS=7000

# Google AI Studio credential: server-only, also configure privately in Vercel.
# Without this key the app is a labelled deterministic preview, not a live AI demo.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite

PORT=4173
