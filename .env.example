# Keep all credentials server-side. Never commit a real value.
NOKIA_NAC_API_KEY=
DECISION_RATE_LIMIT=12

# Optional OpenAI-compatible planner. The deterministic planner is used when absent.
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=

PORT=4173
